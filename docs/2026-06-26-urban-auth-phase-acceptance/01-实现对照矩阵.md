# 实现对照矩阵 — 三仓 vs EPIC

> **对照基线**：[00-EPIC](../2026-06-23-baseplatform-auth-solution/00-EPIC-项目改动总览.md)、[07-缺口对齐](../2026-06-24-buildlicenseno-machinecode-confusion/07-客户端服务端EPIC缺口对齐拟稿提案.md)  
> **图例**：✅ 已实现 · ⚠️ 部分/保留 Legacy · ❌ 未实现 · 🔲 非本阶段范围

---

## 1. BasePlatform.PublicApi

| EPIC / 设计项 | 实现 | 状态 | 备注 |
|---------------|------|------|------|
| `GET /api/auth/license-file`（5001） | `AuthController` | ✅ | `b7e3e01` 路由规范化 |
| `POST /api/auth/activate-urban` | `AuthController` | ✅ | 验 Redis、回写 `MachineCode`、返回 `jwtToken` |
| `ListProjects` 输出 `accessCode` / `machineCode` | `ProjectCatalogController` | ✅ | 移除错误 `BuildLicenseNo` 映射 |
| JWT Claims：`accessCode`, `machineCode`, 无 `fdBuildLicenseNo` | `BasePlatformJwtTokenGenerator` | ✅ | |
| JWT `iss = BasePlatform` | `JwtIssuer = "BasePlatform"` | ✅ | 2026-06-26 与客户端对齐 |
| `SendAuthLicense` 5001 Redis 含 `AccessCode` | 归档 tasks | ✅ | |
| `DownloadUrbanLicense`（Web 离线下载） | BasePlatform Web | ✅ | 运维路径 |
| `POST /api/auth/verify` | — | 🔲 | **刻意不实施** |

**BasePlatform 结论**：P0 开发项 **全部完成**。

---

## 2. UrbanManagement

| EPIC / V2 设计 | 实现 | 状态 | 备注 |
|----------------|------|------|------|
| `GovProject.AccessCode` / `MachineCode` / `AuthToken` | 实体 + 迁移 | ✅ | `BuildLicenseNo` → `AccessCode` |
| `GovProject.LastMachineCodeUpdate` | — | ❌ | P2 可选，EPIC 有字段设计 |
| `FdBuildLicenseNo` 废弃 | Legacy 仍保留部分引用 | ⚠️ | 另立 change |
| JWT 委托 `license-file`（内部） | `GovProjectLicenseAppService` | ✅ | 调 BasePlatform |
| **`GET /api/urban/auth/license-file`** REST 别名 | ABP `gov-project-license/generate` | ❌ | P2，客户端不直连 |
| **`POST /api/urban/auth/activate`** | `UrbanAuthController` | ✅ | `3a17701` |
| 激活后更新 `GovProject.MachineCode` 等 | `UrbanAuthActivateAppService` | ✅ | `[UnitOfWork]` |
| `IBasePlatformAuthHttpClient.ActivateAsync` | Refit | ✅ | `/api/auth/activate-urban` |
| Hub `VerifyJwtAsync` / `GetClientProjectLicenseInfo` | 已实现 | ✅ | |
| Hub `UpdateClientLicense` 推送 | — | ❌ | P1 可选 |
| 单元测试 | `UrbanAuthActivateAppServiceTests` | ✅ | OpenSpec tasks §4 |

**Urban 结论**：**P0 `activate` 代理已完成**（07 拟稿 v0.4 撰写时仍为 ❌，现已闭合）。

---

## 3. MaterialClient.Urban

| EPIC / 06 设计 | 实现 | 状态 | 备注 |
|----------------|------|------|------|
| `LicenseInfo.AccessCode`；删 `AuthToken` / `FdBuildLicenseNo` | EF 迁移 | ✅ | `b4ae3d7` |
| `StaticLicenseChecker`：`iss=BasePlatform` | `ValidIssuer = "BasePlatform"` | ✅ | |
| Refit `POST /api/urban/auth/activate` | `IUrbanAuthApi` | ✅ | `34be994` |
| 禁止 5001 直连 BasePlatform 激活 | 拦截逻辑 | ✅ | |
| 在线激活 UI + 写入 `LatestJwtToken` | `UrbanActivationWindow` + `LicenseService` | ✅ | |
| 默认在线激活、隐藏离线 UI | `UrbanActivationUiOptions` | ✅ | `3ad1daa`；离线逻辑保留 |
| 未授权页机器码复制 | `UnauthorizedNoticeWindow` | ✅ | 随离线 UI 隐藏 |
| 项目信息隐藏接入码行（Urban） | `ProjectInfoWindow` `IsAuthCodeVisible` | ✅ | `da25fb4` |
| SignalR `UpdateClientLicense` handler | 已注册 | ✅ | 等待 Urban 推送 |
| Hub 不同步 `fdBuildLicenseNo` | 已移除 | ✅ | |
| `StaticLicenseChecker` 单元测试 | 用例 Skip | ⚠️ | 待补测 |

**MaterialClient 结论**：**功能开发完成**；JWT 验签单测待补。

---

## 4. 统一契约收口（与 07 §5 对照）

### 4.1 客户端 → Urban

```
POST /api/urban/auth/activate
Body: { "productCode": 5001, "code": "<授权码>", "machineCode": "<机器码>" }
Success data: jwtToken, proId, proName, accessCode, authEndDate
```

**状态**：✅ 三端契约一致。

### 4.2 Urban → BasePlatform（内部）

| 方法 | 路径 | 状态 |
|------|------|------|
| POST | `/api/auth/activate-urban` | ✅ |
| GET | `/api/auth/license-file` | ✅ |

### 4.3 JWT（5001）

| 项 | 设计值 | 实现 | 状态 |
|----|--------|------|------|
| `iss` | `BasePlatform` | 签发端 + 客户端验签一致 | ✅ |
| `aud` | `MaterialClient.Urban` | | ✅ |
| Claims | `proId`, `proName`, `accessCode`, `machineCode`, `exp`, `jti` | | ✅ |

---

## 5. OpenSpec `add-urban-auth-activate-proxy` 任务状态

| 章节 | 内容 | 状态 |
|------|------|------|
| §1 DTO + Refit | UrbanManagement | ✅ |
| §2 AppService | 代理 + GovProject 更新 | ✅ |
| §3 HTTP 路由 | `UrbanAuthController` | ✅ |
| §4 测试 | 单元测试 | ✅ |
| §5 文档 | BasePlatform-JWT-Endpoints.md | ✅ |
| §6 联调 | 端到端（用户负责） | 🔲 待勾选 |

---

## 6. 与 07 拟稿状态差异说明

[07-客户端服务端EPIC缺口对齐拟稿提案](../2026-06-24-buildlicenseno-machinecode-confusion/07-客户端服务端EPIC缺口对齐拟稿提案.md)（v0.4，2026-06-26 撰写）记录：

- P0 Urban `activate` 代理：**❌ 待实现**

**本验收更新**：截至 2026-06-26 末，Urban `3a17701` 已实现该代理，**P0 开发缺口已闭合**。建议将 07 拟稿 §1 表格与 §4.2 修订为 ✅，或引用本验收报告作为 supersede 记录。

---

**文档版本**：1.0  
**最后更新**：2026-06-26
