# 联调验收清单

> **用途**：目标环境（开发/预发）端到端验收，由联调负责人逐项勾选。  
> **对照**：[07 §7 用户验收参考](../2026-06-24-buildlicenseno-machinecode-confusion/07-客户端服务端EPIC缺口对齐拟稿提案.md#7-用户验收参考联调与测试)  
> **前置**：三仓已部署对应提交（见 [00-验收总览](./00-验收总览.md) §4）

---

## 环境信息

| 项 | 填写 |
|----|------|
| 验收环境 | |
| BasePlatform.PublicApi 版本/提交 | `b7e3e01` 或更新 |
| UrbanManagement 版本/提交 | `3a17701` 或更新 |
| MaterialClient 版本/提交 | `3ad1daa`（`feat/modify-auth`）或更新 |
| 验收人 | |
| 验收日期 | |

---

## A. 在线激活主路径（P0）

| # | 场景 | 操作步骤 | 预期结果 | 结果 | 备注 |
|---|------|----------|----------|------|------|
| A1 | 未授权启动 | 删除/清空本地 JWT，启动 Urban 客户端 | 弹出未授权页 + **自动弹出激活窗**；无 `license.urban` 离线说明 | ☐ 通过 ☐ 失败 | UI：`ShowOfflineActivationUi=false` |
| A2 | 生成授权码 | BasePlatform 后台对 5001 项目「生成授权码」 | Redis 有一次性码；48h TTL | ☐ 通过 ☐ 失败 | |
| A3 | 在线激活 | 输入授权码 + 确认机器码，点「激活」 | HTTP 200；提示成功 | ☐ 通过 ☐ 失败 | 联调中曾遇 404，已修路由 |
| A4 | JWT 落库 | 激活成功后查 SQLite `LicenseInfo` | `LatestJwtToken` 有值；`AccessCode`、`ProjectId`、`AuthEndTime` 已同步 | ☐ 通过 ☐ 失败 | |
| A5 | JWT 内容 | 解码 `LatestJwtToken` | `iss=BasePlatform`；含 `accessCode`、`machineCode`、`proId`、`exp` | ☐ 通过 ☐ 失败 | |
| A6 | 重启验签 | 关闭并重新启动客户端 | 直接进入主界面，无未授权页 | ☐ 通过 ☐ 失败 | |
| A7 | Urban 同步 | 查 Urban `GovProject`（按 `proId`） | `MachineCode` 与客户端一致；`AccessCode` / `AuthEndTime` 已更新 | ☐ 通过 ☐ 失败 | |

---

## B. 离线路径（实现保留，UI 隐藏）

| # | 场景 | 操作步骤 | 预期结果 | 结果 | 备注 |
|---|------|----------|----------|------|------|
| B1 | 离线下载 | BasePlatform 录入机器码 → 下载 `license.urban` | 文件为 JWT 明文；`iss=BasePlatform` | ☐ 通过 ☐ 失败 ☐ 跳过 | 运维路径 |
| B2 | 离线导入 | 将 `license.urban` 放程序目录，启动 | 验签通过，进入主界面 | ☐ 通过 ☐ 失败 ☐ 跳过 | UI 未暴露引导 |
| B3 | 未授权页离线区 | 未授权启动 | **不显示**机器码复制区与 `license.urban` 文案 | ☐ 通过 ☐ 失败 | `UrbanActivationUiOptions` |

---

## C. 负向与安全

| # | 场景 | 预期结果 | 结果 | 备注 |
|---|------|----------|------|------|
| C1 | 错误授权码 | 激活失败；不写入 `LatestJwtToken` | ☐ 通过 ☐ 失败 | |
| C2 | 重复使用授权码 | 第二次激活失败（Redis 已删） | ☐ 通过 ☐ 失败 | |
| C3 | `iss=UrbanManagement` 旧 JWT | 客户端 **拒绝** | ☐ 通过 ☐ 失败 | 须重新签发 |
| C4 | 机器码不匹配 | 验签或 Claims 校验失败 | ☐ 通过 ☐ 失败 | |
| C5 | `productCode != 5001` | Urban 代理拒绝 | ☐ 通过 ☐ 失败 | |

---

## D. SignalR 与项目信息（增强）

| # | 场景 | 预期结果 | 结果 | 备注 |
|---|------|----------|------|------|
| D1 | Hub `VerifyJwtAsync` | `ServerJwt` 覆盖本地 | ☐ 通过 ☐ 失败 ☐ 跳过 | |
| D2 | Hub `GetClientProjectLicenseInfo` | `buildLicenseNo` → 客户端 `AccessCode` | ☐ 通过 ☐ 失败 ☐ 跳过 | |
| D3 | 项目信息窗口 | 顶栏点击「项目信息」可打开；Urban 模式**不显示接入码行** | ☐ 通过 ☐ 失败 | |
| D4 | Hub `UpdateClientLicense` | 推送后客户端更新 JWT | ☐ 通过 ☐ 失败 ☐ N/A | Urban **未实现**推送 |

---

## E. 非 5001 回归

| # | 场景 | 预期结果 | 结果 | 备注 |
|---|------|----------|------|------|
| E1 | ProductCode 5000 | 仍走 `GetAuthClientLicense`；**无** `jwtToken` | ☐ 通过 ☐ 失败 ☐ 跳过 | |
| E2 | 其它产品 `SendAuthLicense` | 与现网行为一致 | ☐ 通过 ☐ 失败 ☐ 跳过 | |

---

## 验收汇总

| 类别 | 通过数 | 失败数 | 跳过数 |
|------|--------|--------|--------|
| A 在线激活 | /7 | | |
| B 离线 | /3 | | |
| C 负向 | /5 | | |
| D 增强 | /4 | | |
| E 回归 | /2 | | |

**联调结论**：☐ 通过，可进入发版 ☐ 有条件通过（列明失败项） ☐ 不通过

**失败项与跟进**：

```
（填写）
```

---

**文档版本**：1.0  
**最后更新**：2026-06-26
